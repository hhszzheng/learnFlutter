import 'package:flutter/material.dart';

class YFModelPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('pageModel页面'),
      ),
      body: Center(
        child: Text('Model Page'),
      ),
    );
  }
}
